// use: gradlew wrapper
// run: gradlew clean shadowJar
buildscript {
    ext.kotlin_version = '1.0.6'

    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

plugins {
    id 'com.github.johnrengelman.shadow' version '1.2.3'
    id 'application'
    id 'java'
}

apply plugin: 'kotlin'

sourceSets {
    main.kotlin.srcDirs += 'src/main/kotlin'
    main.resources { "src/main/resources" }
    //main.java.excludes += "src/main/java"
}

group 'com.futuremangaming'
version 'ALPHA'

compileJava.options.encoding = 'UTF-8'
mainClassName = 'com.futuremangaming.futurebot.FutureBotKt'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
    jcenter() // for JDA
    mavenCentral() // for kotlin
    maven {
        url 'http://maven.sedmelluq.com' // for LavaPlayer
    }
}

dependencies {
    //compile fileTree(dir: './libs', include: '*.jar')
    compile 'net.dv8tion:JDA:3.0.BETA2_118'
    compile 'com.sedmelluq:lavaplayer:1.1.36'
    compile 'org.slf4j:slf4j-simple:1.7.22'
    compile "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    //compile group: 'commons-io', name: 'commons-io', version: '2.5'
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.2.1'
}
